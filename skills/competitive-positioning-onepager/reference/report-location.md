# Where the source report lives

Default expected location: the `competitive-intel/` folder inside the user's Cowork working folder
(e.g. `genre-peer-quarterly-analysis.md`, refreshed each quarter by the scheduled research task).

If the file isn't there, ask where it was saved rather than assuming — the working-folder layout is
whatever the user set up when they scheduled the research task, and it may move.
